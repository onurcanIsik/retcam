enum RouteEnums {
  splashPath('/'),
  galleryPath('/gallery'),
  cameraPath('/camera'),
  historyPath('/history'),
  settingsPath('/settings'),
  homePath('/home');

  final String value;
  const RouteEnums(this.value);
}
